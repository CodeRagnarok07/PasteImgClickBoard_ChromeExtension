{
  "name": "Paste from clickboard to Input File",
  "description": "paste images into input files from the clickboard",
  "private": true,
  "version": "0.0.1.5",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "vite": "^5.1.4"
  },
  "dependencies": {
    "chrome-types": "^0.1.270"
  }
}
